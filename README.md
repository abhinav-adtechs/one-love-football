# One Love Football Platform

Base Vite + TypeScript webapp scaffold for the One Love Football community platform.

## Getting started

```bash
npm install
npm run dev
```

## Supabase integration

Create a `.env` file from `.env.example` and add your Supabase project URL and anon key.

We will add auth, storage, and live data modules as we build each feature.
